spring.datasource.url=jdbc:postgresql://${DB_HOST}:${POSTGRES_PORT}/${DB_NAME}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}

spring.jpa.hibernate.ddl-auto=update